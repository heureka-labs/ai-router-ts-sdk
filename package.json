{
  "name": "ai-router-sdk",
  "version": "0.0.1",
  "description": "Official Python SDK for airouter.io - Automatically route requests to the best Large Language Model (LLM).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "format": "prettier --write .",
    "check-exports": "attw --pack .",
    "ci": "npm run build && npm run format && npm run check-exports",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matthias Lau <matthias@airouter.io>, Kai Wendlandt <kai@airouter.io>",
  "license": "ISC",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.17.3",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "openai": "^4.78.1"
  }
}
